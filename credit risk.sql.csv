CREATE VIEW v_clean_credit_risk AS
SELECT 
    person_age,
    person_income,
    person_emp_length,
    loan_amnt,
    loan_int_rate,
    loan_percent_income,
    cb_person_cred_hist_length,
    person_home_ownership,
    loan_intent,
    loan_grade,
    cb_person_default_on_file,
    loan_status
FROM staging_credit_risk
WHERE loan_int_rate IS NOT NULL 
  AND person_emp_length IS NOT NULL
  -- Remove the impossible 144-year-old applicant
  AND person_age < 100
  -- Remove the impossible 123-year employment history (employment must be less than age)
  AND person_emp_length < person_age;

-- 3. Verify it worked by pulling from your new clean view
SELECT * FROM v_clean_credit_risk LIMIT 10;

SELECT COUNT(*) FROM v_clean_credit_risk;

SELECT * FROM v_clean_credit_risk;